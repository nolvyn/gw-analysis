[project]
name = "gw-analysis"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "h5py",
    "ipykernel",
    "lalsuite",
    "matplotlib",
    "numpy",
    "pandas",
    "pycbc",
]

[dependency-groups]
dev = [
    "zenodo-get",
]
